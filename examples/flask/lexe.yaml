project:
  name: flask
  vm-host: lexe-flask

public:
  port: 8000
  healthcheck-url: https://lexe-flask.exe.xyz/

services:
  redis:
  web:
    hooks:
      start-pre: python hooks/log-event.py
      start-post: python hooks/log-event.py

# Spec for `lexe deploy`

## Checklist

- [x] Expand config loading/validation for deploy-only fields
- [x] Define deploy prerequisite checks and failure behavior
- [x] Define release tag generation for the initial deploy phase
- [ ] Define local secret hydration for deploy
- [x] Define local build and image transfer workflow
- [x] Define compose deployment behavior against the provisioned VM
- [x] Define pre-start and post-start hook execution behavior
- [x] Define deploy healthcheck behavior and success criteria for the initial deploy phase
- [ ] Define deploy logging and post-deploy cleanup behavior
- [x] Validate a full deploy flow against `examples/hello`

## References

- Parent spec: `spec/2026-04-18-create-lexe.md`
- Prior work: `spec/2026-04-19-provision.md`

## Current Scope

This spec is only for `lexe deploy`.

It assumes the target VM already exists and has been prepared by `lexe provision`. This spec covers
the local-to-remote deploy flow for a provisioned exe.dev VM, including image build/transfer,
container replacement, and the current health-gated startup path.

### Current Implemented Phase

The current implementation covers the minimum remote deploy flow plus hook execution:

- local image build
- image transfer to the provisioned VM
- remote pre-start hooks using the deployed service image as one-off containers
- remote `docker compose` startup using local `compose.yaml` and `compose.server.yaml`
- remote post-start hooks using the deployed service image as one-off containers
- container health via Docker Compose `--wait` and a compose-defined healthcheck

Still deferred in this phase:

- secrets hydration
- local HTTP healthcheck polling beyond Docker Compose health waiting
- deploy log writing
- remote image prune
- already-deployed no-op behavior and `--force`

## Decisions Captured

- `deploy` does not create or repair the VM; the user runs `provision` first.
- `deploy` requires a clean git working tree by default.
- `deploy --allow-dirty` permits a dirty working tree for intentional non-release deploys.
- Release tags use `vYYYY-MM-DD-<sha7>` from the current git commit SHA.
- Dirty-worktree deploy tags append `-dirty` to the computed release tag.
- The app image is built locally and then transferred to the VM.
- Remote container lifecycle is managed from the local machine against the provisioned VM.
- Image transfer targets the VM with `docker pussh <vm-host>`.
- Remote compose targets the VM through Docker's SSH host support rather than Docker contexts.
- Hook execution uses one-off containers for the configured service image rather than host shell access.
- String hook commands are shell-style commands; list-valued hook commands are treated as argv.
- Pre-start hook failures abort the deploy before service startup.
- Post-start hook failures are surfaced to the operator but do not fail the deploy.
- The current success criterion is Docker Compose health waiting against the service healthcheck.
- Deploys must always use `compose.yaml` and `compose.server.yaml` explicitly.
- `compose.override.yaml` is never part of a deploy.

## Deploy-Specific Config Surface

This spec builds on the minimum config already established for provision work.

The current implementation adds deploy-oriented config parsing for:

- `hooks.pre-start`
- `hooks.post-start`

Current accepted hook command shapes:

- shell command string
- argv-style list of strings
- mapping with explicit `service` and `command`

Deferred config additions for later phases:

- `healthcheck-url`
- `deploy.env-file` with default `deploy/default.env`

## Deploy Repository Contract

The deploy workflow expects these repository artifacts:

- `Dockerfile`
- `compose.yaml`
- `compose.server.yaml`

Deferred repository artifacts for later phases:

- `deploy/default.env` by default, unless overridden in config
- `lexe-logs.md`

## Non-Goals In Scope For This Spec

- No blue/green or canary deploy behavior
- No automatic rollback on deploy failure
- No dry-run mode
- No custom-domain concerns

## Planned Validation

- Re-running deploy for the same commit to confirm the no-op path once that behavior exists
- Forced redeploy of the same release once that behavior exists
- Re-running a dirty deploy for the same commit to confirm it rebuilds and re-transfers
- Pre-start hook failure handling
- HTTP healthcheck failure handling beyond Compose health waiting
- Successful deploy log entry creation

## Validation Notes

- Added focused unit tests for the first deploy phase: CLI wiring, clean-tree enforcement,
  `--allow-dirty` tag suffix behavior, local build/image transfer command sequence, and remote
  compose startup with Docker health waiting.
- Added focused deploy unit tests for hook command normalization, hook execution ordering, and
  post-start report-only failure behavior.
- Added `examples/hello/compose.yaml` and `examples/hello/compose.server.yaml` with a compose-level
  healthcheck so deploy success can be gated on Docker reporting the container healthy.
- Verified the real `examples/hello` integration flow end-to-end with provision, deploy, healthy
  remote compose startup, and VM teardown.
- Expanded `examples/flask` to exercise deploy hooks end-to-end with a shared hook script writing
  ordered event entries, and updated the app to render the recent event log in its response.

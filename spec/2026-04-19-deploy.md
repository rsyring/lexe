# Spec for `lexe deploy`

## Checklist

- [ ] Expand config loading/validation for deploy-only fields
- [ ] Define deploy prerequisite checks and failure behavior
- [ ] Define release tag generation and already-deployed behavior
- [ ] Define local secret hydration for deploy
- [ ] Define local build and image transfer workflow
- [ ] Define compose deployment behavior against the provisioned VM
- [ ] Define pre-start and post-start hook execution behavior
- [ ] Define deploy healthcheck behavior and success criteria
- [ ] Define deploy logging and post-deploy cleanup behavior
- [ ] Validate a full deploy flow against `examples/hello`

## References

- Parent spec: `spec/2026-04-18-create-lexe.md`
- Prior work: `spec/2026-04-19-provision.md`

## Current Scope

This spec is only for `lexe deploy`.

It assumes the target VM already exists and has been prepared by `lexe provision`. This spec covers
the local-to-remote deploy flow for a provisioned exe.dev VM, including image build/transfer,
container replacement, hooks, healthcheck, and deploy logging.

### Initial Implementation Slice

The first implementation is intentionally narrowed to the minimum needed to deploy
`examples/hello`:

- local image build
- image transfer to the provisioned VM
- remote `docker compose` startup using local `compose.yaml` and `compose.server.yaml`
- container health via Docker Compose `--wait` and a compose-defined healthcheck

Deferred from this first slice:

- secrets hydration
- pre-start and post-start hooks
- local HTTP healthcheck polling
- deploy log writing
- remote image prune
- already-deployed no-op behavior and `--force`

## Decisions Captured

- `deploy` does not create or repair the VM; the user runs `provision` first.
- `deploy` requires a clean git working tree by default.
- `deploy --allow-dirty` permits a dirty working tree for intentional non-release deploys.
- Release tags use `vYYYY-MM-DD-<sha7>` from the current git commit SHA.
- Dirty-worktree deploy tags append `-dirty` to the computed release tag.
- If the computed release tag is already present on the remote VM, deploy is a no-op by default.
- `deploy --force` re-runs stop/start, hooks, and healthcheck when the release image for the
  computed tag is already present remotely.
- `deploy --force` does not imply a rebuild or image re-transfer when the exact tag already exists
  on the remote VM.
- `deploy --allow-dirty` must bypass the normal already-deployed no-op path and always perform a
  full deploy, including rebuild and image re-transfer, because the dirty tag is not unique across
  local uncommitted changes.
- The app image is built locally and then transferred to the VM.
- Remote container lifecycle is managed from the local machine against the provisioned VM.
- Deploys must always use `compose.yaml` and `compose.server.yaml` explicitly.
- `compose.override.yaml` is never part of a deploy.
- Secrets are hydrated locally from the committed env template before remote containers are created.
- Pre-start hooks run before service startup and abort deploy on failure.
- Post-start hooks run after startup and are report-only.
- Deploy success requires the configured `healthcheck-url` to return a 2xx response.
- Successful deploys append an entry to `lexe-logs.md`.
- Successful deploys also perform remote image cleanup for dangling layers.

## Deploy-Specific Config Surface

This spec builds on the minimum config already established for provision work.

Deploy adds or depends on these fields:

- `healthcheck-url`
- `deploy.env-file` with default `deploy/default.env`
- `hooks.pre-start`
- `hooks.post-start`

## Deploy Repository Contract

The deploy workflow expects these repository artifacts:

- `Dockerfile`
- `compose.yaml`
- `compose.server.yaml`
- `deploy/default.env` by default, unless overridden in config
- `lexe-logs.md`

## Non-Goals In Scope For This Spec

- No blue/green or canary deploy behavior
- No automatic rollback on deploy failure
- No dry-run mode
- No custom-domain concerns

## Planned Validation

- First deploy to a freshly provisioned VM
- Re-running deploy for the same commit to confirm the no-op path
- Forced redeploy of the same release
- Re-running a dirty deploy for the same commit to confirm it rebuilds and re-transfers
- Pre-start hook failure handling
- Healthcheck failure handling
- Successful deploy log entry creation

## Validation Notes

- Added focused unit tests for the first deploy slice: CLI wiring, clean-tree enforcement,
  `--allow-dirty` tag suffix behavior, local build/image transfer command sequence, and remote
  compose startup with Docker health waiting.
- Added `examples/hello/compose.yaml` and `examples/hello/compose.server.yaml` with a compose-level
  healthcheck so deploy success can be gated on Docker reporting the container healthy.
